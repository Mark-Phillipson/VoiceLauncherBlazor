# Outgoing Message Preview

Generated: 2026-05-29

Purpose: provide a compact summary of recent work and a ready-to-edit preview of the exact messages the app will send next. Edit this file to weed out or adjust content before asking me to "send" or run the next operation.

---

**Compact Conversation Summary**
- **Scope:** cleaned noisy raw hardware rows and added file-aware descriptions for foot-switch Talon files.
- **Backend changes:** `DerivePlainLanguageDescriptionAsync` now prefers file-name-based foot-switch descriptions; `IsRawHardwareTrigger` excludes foot-switch files.
- **UI changes:** "What would you say?" quiz gains a repository/source selector; `GenericQuiz` label updated to `Source scope`.
- **DB work:** backfill run via POST `/admin/recreate-all-descriptions` — ~4996 rows updated; sample row now reads: "click the right mouse button with foot_switch_FS3.talon (on windows)".

---

**Outgoing message templates**

1) Quiz question (what the UI will display / send to the learner)

Template:

```json
{
  "Prompt": "What voice command would you use to {plain}{context}?",
  "Choices": ["choiceA", "choiceB", "choiceC", "choiceD"],
  "CorrectAnswer": "<correct-command>",
  "RelatedCommandId": <id>
}
```

Sample (derived from a recently-updated DB row):

```json
{
  "Prompt": "What global voice command would you use to show the mouse grid on the second (right) screen?",
  "Choices": ["grid right", "grid left", "show mouse grid", "show grid on primary screen"],
  "CorrectAnswer": "grid right",
  "RelatedCommandId": 424556
}
```

2) Description update (what the backfill/DB update will write)

Template (HTTP/DB payload):

```json
{
  "Id": <id>,
  "Description": "<derived plain-language description>"
}
```

Sample (from backfill run):

```json
{
  "Id": 424556,
  "Description": "show the mouse grid on the second (right) screen"
}
```

3) Admin endpoint (triggering a full backfill)

The running admin endpoint used previously takes no body and is invoked as:

```text
POST http://localhost:5008/admin/recreate-all-descriptions
```

Response example:

```json
{ "updated": 4996 }
```

---

How to use this file:
- Edit the sample messages above to remove or change any phrasing you don't want shown or stored.
- Tell me "send" or "apply backfill" when you're ready and I will use the exact contents here to run the next operation (I will show a final confirmation before any DB writes).

Notes:
- The quiz `Prompt` is generated by `BuildQuestionFromVoiceCommand` in `RazorClassLibrary/Pages/QuizHub.razor`.
- The derived description logic lives in `DataAccessLibrary/Services/TalonVoiceCommandDataService.cs`.
