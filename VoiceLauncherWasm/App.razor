<!-- DEBUG: temporarily render a minimal UI to check whether Blazor can render at all -->
<div class="container mt-5">
    <h1>Debug: Blazor runtime rendered this static content</h1>
    <p>If you see this, the runtime and component rendering are working — the problem is likely in routing or a page component.</p>
</div>
