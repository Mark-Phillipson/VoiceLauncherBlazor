CREATE TABLE [dbo].[MultipleLauncher] (
    [ID]          INT           IDENTITY (1, 1) NOT NULL,
    [Description] NVARCHAR (70) NOT NULL,
    CONSTRAINT [PK_dbo.MultipleLauncher] PRIMARY KEY CLUSTERED ([ID] ASC)
);

