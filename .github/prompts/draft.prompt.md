
 can we add an access key to theCommand line TextBox

  also can we add an access key to the back button

  Also can we have an access key for the  type to filter images TextBox

   and the mark as favorite checkbox