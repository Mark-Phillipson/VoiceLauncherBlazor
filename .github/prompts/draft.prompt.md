
I would like to make this application installable on other computers given that it has a database what steps should require this to happen as people would not even have SQL Server installed for example

Now what were using SQLite three can we work on away of creating a installable product for other computers
